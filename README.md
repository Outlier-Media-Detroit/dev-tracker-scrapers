# Development Tracker Scrapers

Web scrapers for different sources used in Outlier Media's Development Tracker.

## Setup

```bash
poetry install
poetry run pytest
poetry run scrapy crawl <spider_name>
```
